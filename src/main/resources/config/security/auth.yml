security:
  # Set to true in production (requires a valid SSL certificate)
  hsts:
    enabled: false

  login-attempts:
    username:
      max-attempts: 5
      lockout-minutes: 15
    ip:
      max-attempts: 10
      lockout-minutes: 60
    combined:
      max-attempts: 3
      lockout-minutes: 5

