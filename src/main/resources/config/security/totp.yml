app:
  security:
    totp:
      issuer: "YourApp"
      time-step: 30
      time-skew-tolerance: 1
      code-digits: 6
      algorithm: "SHA1"
      secret-length: 160
      qr-code:          # ← QR code spécifique TOTP
        size: 200
        format: "PNG"